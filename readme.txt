For compiling use:
  
  javac dblpParser/Parser2.java

on the dblpParser parent directory.

For running use:
  
  java -cp . -Xmx4G -DentityExpansionLimit=10000000 dblpParser.Parser2 2016

on the same directory.
